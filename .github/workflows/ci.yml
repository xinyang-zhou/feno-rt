name: tests

on:
  push:
  pull_request:

jobs:
  cpu:
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.9"
          cache: pip
      - name: Install CPU runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -r requirements/runtime.lock
          python -m pip install -r requirements/test.lock
          python -m pip install -e . --no-deps
      - name: Run core tests
        run: python -m unittest discover -s tests -v
      - name: Run core CPU benchmark
        run: python benchmarks/benchmark_core.py --device cpu --repeats 3
      - name: Check full engine diagnostic artifacts
        run: |
          python benchmarks/profile_engine.py --toy --device cpu --requests 16 --output-dir /tmp/feno-engine-ci
          python benchmarks/summarize_engine_profile.py /tmp/feno-engine-ci
      - name: Verify source and wheel builds
        run: |
          python -m pip install build
          python -m build
