"""FENO-RT: cache-aware scientific neural-operator inference runtime.

Model classes live in :mod:`feno_rt.models`; keeping the package root light avoids
loading PyTorch when scripts only need project paths or metadata.
"""

__version__ = "0.9.0.dev0"
